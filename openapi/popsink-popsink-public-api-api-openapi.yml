openapi: 3.1.0
info:
  title: Fast admin Popsink Public API API
  version: 0.1.0
servers:
- url: /api
tags:
- name: Popsink Public API
paths:
  /:
    get:
      summary: Root
      description: Root message.
      operationId: root__get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema: {}
      tags:
      - Popsink Public API
components:
  securitySchemes:
    OAuth2PasswordBearer:
      type: oauth2
      flows:
        password:
          scopes: {}
          tokenUrl: auth/jwt/login
